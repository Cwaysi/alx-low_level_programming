#include "main.h"
/**
 * _abs - Checck for absolute value
 * @r: An integer
 * Description: All desc
 * Return: absolute
 */

int _abs(int)
{
	if (r >= 0)
	{
		return (r);
	}
	else
		return (r * -1);
}
